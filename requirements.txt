# Build tooling first (avoids "Cannot import setuptools.build_meta" on Render)
setuptools>=65.0.0
wheel>=0.38.0

Django>=6.0.0
gunicorn>=21.0.0
djangorestframework==3.14.0
django-cors-headers==4.3.1
python-dotenv==1.0.0
channels==4.1.0
pymongo==4.6.0
mongoengine==0.27.0
django-environ==0.11.2
# PostgreSQL driver (when DATABASE_URL is set and not using MongoDB as Django DB)
psycopg2-binary>=2.9.9
# Django ORM → MongoDB (set DJANGO_USE_MONGODB=true + MONGODB_URI). Match Django 6.0.x.
django-mongodb-backend>=6.0.2,<6.1
Pillow>=12.0.0
google-generativeai==0.3.2
requests==2.31.0
pytesseract==0.3.10
# Pin numpy first so pip uses wheel (1.26.4); old numpy builds fail on Python 3.12 (pkgutil.ImpImporter removed)
numpy==1.26.4
# 4.10+ has wheels for Python 3.12 and works with numpy 1.26; older 4.8.x can pull numpy 1.24 and trigger source build
opencv-python>=4.10.0