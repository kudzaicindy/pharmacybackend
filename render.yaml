# Render Blueprint: Django app (not FastAPI). Use this when creating a new Web Service.
services:
  - type: web
    name: pharmacybackend
    runtime: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn pharmacybackend.wsgi:application --bind 0.0.0.0:$PORT
